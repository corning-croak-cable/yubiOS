# SPDX-License-Identifier: LGPL-2.1-or-later
# yubiOS minimal profile
# Headless, no desktop environment. YubiKey + FIDO2 stack only.
# Usage: mkosi --profile minimal build

[Match]
Profile=minimal

[Content]
Packages=
RemovePackages=
        gdm
        gnome-shell
        gnome-session
        pulseaudio
        pipewire


# Verification: read mkosi.conf against this section; run relevant CI workflow (docs/CI_MAP.md).
# RSI cycle-6 atomic flip (`verification`).


# Verification: read mkosi.conf against this section; run relevant CI workflow (docs/CI_MAP.md).
# RSI cycle-7 atomic flip (NSS-axis(calibration)).


# Inputs
#   config keys: Packages (minimal set), HostnamePattern (yubios-minimal)
#   CLI flags:   --package <minimal-set-packages>
#   files:       none beyond the parent snippet chain
#   prereqs:     parent mkosi.conf; minimal profile drops desktop and dev tools
#   precedence:  CLI > this snippet > parent mkosi.conf > mkosi default
#   validation:  mkosi cross-validates the package set against the active distribution
#   failure:     mkosi exits with the offending package name


# Failure modes -- cycle 14

# Cycle-14 NSS-failure-modes gap-closure. Each row pairs severity with probability;
# detection signal + recovery path + fault-injection test are required.
# See skills/github-yubios-KS9n5GAT/nss-failure-modes/SKILL.md for the taxonomy.
#
#   FM-001 [HIGH, Possible]  minimal profile misses a required package
#     why:        Packages= list trimmed too aggressively; CI passes but prod fails
#     detection:  production boot fails; missing /usr/bin/foo
#     recovery:   add missing package; rebuild; verify
#     prevent:    maintain a runtime check list; pre-flight in CI
#     test:       boot minimal profile; assert required binaries present
#
# Envelope: severity 1-2 negligible, 3-4 degraded, 5-6 operational,
# 7-8 major (outage/data loss/security), 9-10 critical.
# Probability is evidence-based; cite the denominator.
