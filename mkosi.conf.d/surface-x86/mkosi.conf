# SPDX-License-Identifier: LGPL-2.1-or-later
# yubiOS surface-x86 profile — Microsoft Surface (Intel/AMD x86-64)
# Targets: Surface Pro 9 (Intel), Surface Laptop 5, Surface Go 3
# Usage: mkosi --profile surface-x86 build

[Match]
Profile=surface-x86
Architecture=x86-64

[Content]
Repositories=linux-surface
Packages=
        kernel-surface
        iptsd
        surface-control
        libwacom
        ipu6-camera-hal
        intel-ipu6-dkms
        linux-firmware
        thermald
        power-profiles-daemon

KernelCommandLine=
        nxcompat=0
        intel_iommu=on iommu=pt


## New Ideas -- cycle 3 (lens external)

This file's lens is **L494** in `lenses.json` (score 6/50, verdict **NO**, k=1/9). Full experiment: hypothesis `mkosi.conf.d/surface-x86/mkosi.conf covers all 9 primitives in the internal-big-picture basis`, method `9-D primitive binarization (purpose, examples, guidelines, constraints, verifica...`. See root `new-ideas-2026-08-12.md` for the cycle-3 summary.
