# SPDX-License-Identifier: LGPL-2.1-or-later
# yubiOS surface-x86 profile — Microsoft Surface (Intel/AMD x86-64)
# Targets: Surface Pro 9 (Intel), Surface Laptop 5, Surface Go 3
# Usage: mkosi --profile surface-x86 build

[Match]
Profile=surface-x86
Architecture=x86-64

[Content]
Repositories=linux-surface
Packages=
        kernel-surface
        iptsd
        surface-control
        libwacom
        ipu6-camera-hal
        intel-ipu6-dkms
        linux-firmware
        thermald
        power-profiles-daemon

KernelCommandLine=
        nxcompat=0
        intel_iommu=on iommu=pt


# Verification: read mkosi.conf against this section; run relevant CI workflow (docs/CI_MAP.md).
# RSI cycle-6 atomic flip (`verification`).


# Verification: read mkosi.conf against this section; run relevant CI workflow (docs/CI_MAP.md).
# RSI cycle-7 atomic flip (NSS-axis(calibration)).


# Failure modes -- cycle 14

# Cycle-14 NSS-failure-modes gap-closure. Each row pairs severity with probability;
# detection signal + recovery path + fault-injection test are required.
# See skills/github-yubios-KS9n5GAT/nss-failure-modes/SKILL.md for the taxonomy.
#
#   FM-001 [MEDIUM, Possible]  Surface x86 profile fails on non-Surface hardware
#     why:        firmware blobs Surface-specific; generic x86 fails
#     detection:  boot hangs at firmware load
#     recovery:   use generic x86 profile; document Surface-specific behavior
#     prevent:    hardware-detection step in profile; clear failure on wrong hw
#     test:       boot Surface profile on generic x86; assert clear fail message
#
# Envelope: severity 1-2 negligible, 3-4 degraded, 5-6 operational,
# 7-8 major (outage/data loss/security), 9-10 critical.
# Probability is evidence-based; cite the denominator.
