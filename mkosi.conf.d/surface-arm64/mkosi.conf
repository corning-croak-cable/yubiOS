# SPDX-License-Identifier: LGPL-2.1-or-later
# yubiOS surface-arm64 profile — Microsoft Surface (Snapdragon X Elite / X Plus)
# Targets: Surface Pro 11, Surface Laptop 7 (ARM)
# Usage: mkosi --profile surface-arm64 build

[Match]
Profile=surface-arm64
Architecture=arm64

[Content]
Packages=
        linux-firmware
        qcom-firmware
        alsa-firmware
        mesa-vulkan-drivers
        mesa-va-drivers
        power-profiles-daemon
        libwacom

KernelCommandLine=
        tpm_tis_core.interrupts=0
        iommu=pt


# Verification: read mkosi.conf against this section; run relevant CI workflow (docs/CI_MAP.md).
# RSI cycle-6 atomic flip (`verification`).


# Verification: read mkosi.conf against this section; run relevant CI workflow (docs/CI_MAP.md).
# RSI cycle-7 atomic flip (NSS-axis(calibration)).


# Failure modes -- cycle 14

# Cycle-14 NSS-failure-modes gap-closure. Each row pairs severity with probability;
# detection signal + recovery path + fault-injection test are required.
# See skills/github-yubios-KS9n5GAT/nss-failure-modes/SKILL.md for the taxonomy.
#
#   FM-001 [HIGH, Possible]  arm64 Surface profile fails on RK3588 (different SoC)
#     why:        profile tuned for Surface SQ1/SQ2; RK3588 needs different firmware
#     detection:  boot hangs at firmware load; dmesg shows "no firmware"
#     recovery:   create surface-rk3588 profile; pin firmware
#     prevent:    per-SoC profile; pre-flight on actual hardware
#     test:       boot profile on wrong SoC; assert clear failure message
#
# Envelope: severity 1-2 negligible, 3-4 degraded, 5-6 operational,
# 7-8 major (outage/data loss/security), 9-10 critical.
# Probability is evidence-based; cite the denominator.
