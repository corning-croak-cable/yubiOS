# SPDX-License-Identifier: LGPL-2.1-or-later
# yubiOS surface-arm64 profile — Microsoft Surface (Snapdragon X Elite / X Plus)
# Targets: Surface Pro 11, Surface Laptop 7 (ARM)
# Usage: mkosi --profile surface-arm64 build

[Match]
Profile=surface-arm64
Architecture=arm64

[Content]
Packages=
        linux-firmware
        qcom-firmware
        alsa-firmware
        mesa-vulkan-drivers
        mesa-va-drivers
        power-profiles-daemon
        libwacom

KernelCommandLine=
        tpm_tis_core.interrupts=0
        iommu=pt


## New Ideas -- cycle 3 (lens external)

This file's lens is **L493** in `lenses.json` (score 6/50, verdict **NO**, k=1/9). Full experiment: hypothesis `mkosi.conf.d/surface-arm64/mkosi.conf covers all 9 primitives in the internal-big-picture basis`, method `9-D primitive binarization (purpose, examples, guidelines, constraints, verifica...`. See root `new-ideas-2026-08-12.md` for the cycle-3 summary.
