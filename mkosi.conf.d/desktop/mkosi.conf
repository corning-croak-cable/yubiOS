# SPDX-License-Identifier: LGPL-2.1-or-later
# yubiOS desktop profile — GNOME on Wayland
# Usage: mkosi --profile desktop build

[Match]
Profile=desktop

[Content]
Packages=
        gnome-shell
        gnome-session
        gdm
        gnome-control-center
        gnome-terminal
        nautilus
        gnome-keyring
        pipewire
        wireplumber
        seahorse

[Runtime]
RuntimeSize=40G


## New Ideas -- cycle 3 (lens external)

This file's lens is **L491** in `lenses.json` (score 6/50, verdict **NO**, k=1/9). Full experiment: hypothesis `mkosi.conf.d/desktop/mkosi.conf covers all 9 primitives in the internal-big-picture basis`, method `9-D primitive binarization (purpose, examples, guidelines, constraints, verifica...`. See root `new-ideas-2026-08-12.md` for the cycle-3 summary.
