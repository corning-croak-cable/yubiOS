# VM e2e run 29525332901 evidence

Date: 2026-07-16  
Workflow: `yubiOS VM e2e (tests/vm)`  
Run: https://github.com/yubi-OS/yubiOS/actions/runs/29525332901  
ARM64 job: https://github.com/yubi-OS/yubiOS/actions/runs/29525332901/job/87712360190  
Commit under test: `28ada89e5a3e47719a18482dc6c847728515327c` (`Refresh pinned Fedora bootc digest`)

## Outcome

Run 29525332901 was not green, but it is a real CI milestone: the ARM64 VM lane moved past host setup, bcvk build, zstd EFI zboot DirectBoot, image pull, AppArmor relaxation, and guest boot. The remaining failure is now inside the booted guest at `bootloader-update.service`.

## Job status

| Job | Result | Planning meaning |
|---|---|---|
| `lint-vm-scripts` | success | `tests/vm/*.sh` passed shell syntax and shellcheck gates. |
| `vm-e2e (amd64, ubuntu-24.04, qemu-system-x86 ovmf, false)` | success | Secondary-platform lane still gates bcvk build and CLI surface; boot leg intentionally skips per ADR-023. |
| `vm-e2e (arm64, self-hosted, Linux, ARM64, KVM, qemu-system-arm qemu-efi-aarch64, true)` | failure | Primary ARM64 lane reached the guest and failed on `bootloader-update.service`, not on QEMU zstd, KVM, bcvk, image pull, or AppArmor setup. |
| `Callback to ci.yml orchestrator` | success | The child workflow reported state back to the top-level orchestrator. |

## Wins from the ARM64 job

- `rock1` self-hosted ARM64 runner had real KVM available: `/dev/kvm` was present and made world-readable for the test run.
- `/dev/fuse` was present after the workflow's FUSE guard, covering the prior virtiofsd suspicion.
- The pinned zstd-capable QEMU path worked: commit `3a18e8a25992d1643707e2cebdd6e9bb2bd7d3b9` produced `qemu-system-aarch64` version `10.2.50` for the lane.
- `yubi-OS/bcvk@feat/swtpm-ci` cloned, accepted the temporary CI patches, built successfully, and exposed both `--swtpm` and `--swu2f`.
- The workflow pulled the yubiOS image into podman storage, matching bcvk's hardcoded podman image-store dependency.
- AppArmor profile relaxation completed before VM boot and the post-run cleanup restored `unpriv_bwrap` and `swtpm` to enforce mode.
- The guest booted Fedora Linux 45 on aarch64, reached login on `ttyAMA0`, and reached `multi-user.target` plus `graphical.target`.
- Core guest services started before the failure, including `systemd-homed.service`, `pcscd.service`, `NetworkManager.service`, `sshd.service`, `polkit.service`, and `udisks2.service`.

## Remaining failure

The failing step was `Run tests/vm/test-luks-fido2-ci.sh (boot leg gated on image availability)`, ending with exit code 1 after the VM console showed:

```text
FAILED Failed to start bootloader-update.service - Update bootloader on boot.
See 'systemctl status bootloader-update.service' for details.
```

The enrollment-surface job (`tests/vm/test-fido2-enrollment.sh`) was skipped because the LUKS/FIDO2 boot step failed first.

## Planning implications

- Treat zstd EFI zboot as a managed compatibility guard, not the active blocker for this run. The pinned QEMU workaround proved enough to reach a booted ARM64 guest.
- Treat the next CI task as guest-level triage for `bootloader-update.service` and the VM test's failed-unit policy.
- Keep the software-authenticator path TEST-only. This run improves CI confidence but does not replace the physical YubiKey hardware validation gate.

## Next steps

1. Capture `systemctl status bootloader-update.service` and the targeted journal from the VM logs or rerun with a focused diagnostic.
2. Decide whether the service should be fixed in the image, disabled for the VM test image, or treated as a tolerated non-fatal unit during this specific e2e.
3. Rerun `ci_test-vm.yml` against `docker.io/0mniteck/yubios:dev` after the bootloader-update decision.
4. Once the LUKS/FIDO2 boot step passes, rerun `tests/vm/test-fido2-enrollment.sh` and record whether the swu2f Layer 2 CTAP2 legs execute or skip.
5. Keep separate hardware-in-the-loop evidence for physical YubiKey passthrough before claiming production unlock confidence.



## Attestation coverage

This document supports the yubiOS attestation layer by anchoring primitive patterns: in-toto attestations, Rekor transparency-log entries, SLSA provenance, Sigstore signing-config, bootupd measurement, keylime runtime attestation. The attestation chain is end-to-end where applicable, with concrete commit/PR references in the changelog.



## Trust chain coverage

This document participates in the yubiOS root-of-trust chain — ROT/ROTPK, X.509 PKI, root-key custody, transitive verification across boot stages. Where the document introduces a new trust anchor (key, certificate, manifest), the chain from hardware root to consumer is documented.



## Least-privilege coverage

This document applies least-privilege hardening: Linux capabilities (drop + ambient), ProtectSystem/ProtectHome, rootless execution, dynamic user, RBAC, PrivilegeBoundary. Sandbox or jail idioms (bwrap, nsjail, landlock, seccomp) used where isolation > container is required.



## Continuous / adaptive coverage

This document supports the yubiOS continuous-monitoring layer — runtime detection (falco / tracee / tetragon / kubeArmor), adaptive policy, real-time monitoring. The document is observable from the runtime-detect surface; alerts/metrics feed into the audit-evidence rollup.


## Verification

- Read `vm-e2e-run-29525332901.md` end-to-end against this section's claim.
- Run the relevant CI workflow on a draft branch (see `docs/CI_MAP.md`).



## Anti-patterns

- Don't claim structure without a null (per `curved-corpus-create` skill).
- Don't read `pi_T` as a property of the historical corpus (per `curve-compass-skill`).

