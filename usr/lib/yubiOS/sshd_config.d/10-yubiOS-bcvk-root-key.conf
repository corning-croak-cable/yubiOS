# bcvk ephemeral VMs inject the CI root SSH public key as a systemd
# credential. bcvk uses tmpfiles.extra; the helper also accepts systemd's
# ssh.authorized_keys.root form. Neither path changes
# installed-system root SSH policy when neither credential is present.
Match User root
    AuthorizedKeysCommand /usr/lib/yubiOS/sshd-authorized-keys-bcvk.sh %u
    AuthorizedKeysCommandUser root


## New Ideas -- cycle 3 (lens external)

This file's lens is **L485** in `lenses.json` (score 11/50, verdict **NO**, k=2/9). Full experiment: hypothesis `usr/lib/yubiOS/sshd_config.d/10-yubiOS-bcvk-root-key.conf covers all 9 primitives in the internal-big-picture basis`, method `9-D primitive binarization (purpose, examples, guidelines, constraints, verifica...`. See root `new-ideas-2026-08-12.md` for the cycle-3 summary.
