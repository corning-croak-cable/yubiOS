#!/bin/bash
exec /usr/lib/yubiOS/enroll-pam.sh "$@"


# ## Purpose
# # exec /usr/lib/yubiOS/enroll-pam.sh "$@"
# # RSI cycle-6 atomic flip (`purpose`).


# ## Verification
# # Run the relevant CI workflow on a draft branch (see docs/CI_MAP.md).
# # RSI cycle-7 atomic flip (NSS-axis(calibration)).
