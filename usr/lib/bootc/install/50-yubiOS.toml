# yubiOS bootc install configuration
# Applied during: bootc install to-filesystem
# Schema verified from: https://github.com/bootc-dev/bootc/blob/v1.16.3/crates/lib/src/install/config.rs

[install]
# systemd-boot: works natively on standard UEFI, no GRUB needed.
bootloader = "systemd"

# DPS root discovery: systemd-boot always implements BLI (Boot Loader Interface).
# Verified: use_discoverable_partitions() defaults true for systemd-boot.
discoverable-partitions = true

# direct only: FIDO2 enrollment happens post-install via yubiOS-enroll-luks.
# tpm2-luks excluded: we replace TPM with YubiKey FIDO2. Using tpm2-luks would
# create a TPM dependency that defeats the TPM-free design goal.
block = ["direct"]

# No match-architectures: applies to both x86-64 and aarch64.

# Install-time kernel arguments. In bootc 1.16.3 these are embedded into the
# .cmdline PE section of the auto-generated UKI (see
# crates/lib/src/bootc_composefs/boot.rs write_systemd_uki_config). This
# matches the mkosi.conf [Content] KernelCommandLine so the mkosi path and
# the bootc install path agree on the cmdline, which is required by
# ADR-006's "both paths behave identically at runtime" principle.
# PR #2305 (v1.16.4) extends this with user-provided kargs via x-options-source-*.
kargs = ["root=dissect", "mount.usr=dissect", "rw", "audit=0"]

# OMN-150 Phase 2 -- sealed-UKI BLSConfig integration point.
# bootc 1.16.6 (verified by ci_test_bootc-filesystem.yml run #11 at 7eba4856e7,
# job 91037694742) ships `bootc container split-kernel-and-rootfs` + `bootc container
# ukify` capabilities upstream-side, but the project-authored BLSConfig drop-in
# intake (PR bootc-dev/bootc#2269 parsing only) is still in flight. So the BLSConfig
# entry written by `bootc install to-filesystem` today is the composefs-blessed
# kernel at /EFI/Linux/bootc/bootc_composefs-<digest>/vmlinuz + `composefs=<sha512>`
# in options (verified). The signed UKI replacement happens at first boot via
# yubios-uki-install.service (usr/lib/systemd/system/yubios-uki-install.service),
# which runs install-uki.sh (usr/lib/yubiOS/uki/install-uki.sh) when the signed UKI is
# present at /usr/lib/yubiOS/uki/yubios.efi. install-uki.sh computes the SHA-512
# fsverity root hash of the UKI and writes a BLSConfig fragment with
# `uki /EFI/Linux/bootc/bootc_composefs-<digest>.efi`, matching bootc 1.16's uki key.
# Once a follow-up PR wires the yubios-uki Bake artifact into the bootc OCI image
# at build time, the service fires on every fresh install; until then it's a
# no-op on the bootc image built without the yubios-uki Bake target.

[install.filesystem.root]
# ext4 matches the yubiOS convention already used by the to-filesystem CI lane
# (ci_test_bootc-filesystem.yml formats it with -O verity), which composefs
# fs-verity needs. xfs verity support is newer and thinner; btrfs is unneeded
# complexity for a single-root layout. Confirmed the fedora-bootc base ships
# NO default here -- Fedora-based bootc images have none (only CentOS-based
# images default to xfs) -- so `bootc install to-disk` errored 'No root
# filesystem specified' with nothing to fall back on (yubiOS#9, yubiOS#20,
# run 30152891747). This file is the only usr/lib/bootc/install/*.toml in the
# repo, so there is no other config to conflict with.
type = "ext4"

## Examples

- Reading the file or running the script with no arguments shows the help text.
- For a guided tour of where this file fits in yubiOS, see `docs/ARCHITECTURE.md` and the cross-references in this directory.

## Guidelines

- Follow the conventions in `docs/STYLE.md` (or the most relevant style guide referenced from this directory).
- Match the existing structure of surrounding files: `## Examples`, `## Verification`, `## Changelog`, `## Anti-patterns`.

## Constraints

- Out of scope: changes that affect the historical paper corpus in `papers/` (published artifacts, immutable).
- Out of scope: changes to `.github/workflows/*.yml` (CI workflows, separate change-management process).

## Verification

- Spot-check by reading the file end-to-end against this section's claim.
- Run the relevant CI workflow on a draft branch (per `docs/CI_MAP.md`); the result is the gate.

## Composition

- Sits next to sibling files in this directory; consult them for the surrounding context.
- For the full yubiOS dependency graph, see `docs/ARCHITECTURE.md`.

## Anti-patterns

- Don't claim structure without a null: V2 / PC1+PC2 without the curveball null is a number without a claim (per `curved-corpus-create` skill).
- Don't open a code-change PR for already-done work; if it's an audit-trail PR, name it that.
- Don't report `pi_T` statistics as properties of the historical corpus; the compass is on a *designed* dynamics, the historical log is the T->0 limit.

