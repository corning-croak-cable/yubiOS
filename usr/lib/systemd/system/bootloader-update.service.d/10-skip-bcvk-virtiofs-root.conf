# bcvk ephemeral DirectBoot uses a virtiofs root and has no persistent
# bootloader or EFI System Partition for bootupd to update. Keep Fedora
# bootloader updates enabled on real installed systems, but skip this
# oneshot service in the CI/direct-boot harness.
[Unit]
ConditionKernelCommandLine=!rootfstype=virtiofs
