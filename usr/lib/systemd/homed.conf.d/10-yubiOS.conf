# SPDX-License-Identifier: LGPL-2.1-or-later
# yubiOS systemd-homed defaults
# Source: https://www.man7.org/linux/man-pages/man5/homed.conf.5.html

[Home]
# LUKS2 loopback file per user — strongest encryption, YubiKey FIDO2 unlockable.
# Source: homectl(1) --storage=luks
DefaultStorage=luks

# btrfs inside LUKS2: quota, snapshots, efficient copy-on-write.
# Source: homectl(1) --fs-type=btrfs
DefaultFileSystemType=btrfs


## New Ideas -- cycle 3 (lens external)

This file's lens is **L507** in `lenses.json` (score 6/50, verdict **NO**, k=1/9). Full experiment: hypothesis `usr/lib/systemd/homed.conf.d/10-yubiOS.conf covers all 9 primitives in the internal-big-picture basis`, method `9-D primitive binarization (purpose, examples, guidelines, constraints, verifica...`. See root `new-ideas-2026-08-12.md` for the cycle-3 summary.
