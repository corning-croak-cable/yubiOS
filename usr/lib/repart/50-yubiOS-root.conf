# yubiOS systemd-repart partition layout
# This is the separate mkosi/DDI fixed-image path: EROFS data is paired with a
# dm-verity hash partition. It is not the writable ext4/Btrfs physical sysroot
# consumed by `bootc install to-filesystem --composefs-backend`.
[Partition]
Type=root
Label=root
Format=erofs
Minimize=best
ReadOnly=yes
Verity=data
VerityMatchFor=root-verity

[Partition]
Type=root-verity
Label=root-verity
Verity=hash
VerityMatchFor=root
SplitName=verity


## New Ideas -- cycle 3 (lens external)

This file's lens is **L506** in `lenses.json` (score 6/50, verdict **NO**, k=1/9). Full experiment: hypothesis `usr/lib/repart/50-yubiOS-root.conf covers all 9 primitives in the internal-big-picture basis`, method `9-D primitive binarization (purpose, examples, guidelines, constraints, verifica...`. See root `new-ideas-2026-08-12.md` for the cycle-3 summary.
