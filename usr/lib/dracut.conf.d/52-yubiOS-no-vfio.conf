# ADR-024 rule 1: default yubiOS images ship virtio-gpu only. Keep VFIO
# drivers out of the initramfs entirely so no boot-time hook can bind them
# before the modprobe.d blacklist in usr/lib/modprobe.d/50-yubiOS-no-vfio.conf
# is even consulted.
# See refs/vgpu-vfio-user-trust-boundary-2026-07-25.md.
omit_drivers+=" vfio vfio_pci vfio_pci_core vfio_iommu_type1 vfio_virqfd "


## New Ideas -- cycle 2 (lens format)

This file was processed by the RSI cycle-2 lens generator (curve-compass-skill v1.1.0 + curved-corpus-create v1.1.0). Each cycle-2 patch IS a concrete experiment with a measurable delta -- not a templated section. The lens below documents the measured dynamics; the patch is the lens, not prose about the file.

```json
{
  "lens": "L506",
  "file": "usr/lib/dracut.conf.d/52-yubiOS-no-vfio.conf",
  "hypothesis": "usr/lib/dracut.conf.d/52-yubiOS-no-vfio.conf covers all 9 primitives in the internal-big-picture basis",
  "method": "9-D primitive binarization (purpose, examples, guidelines, constraints, verification, composition, changelog, references, anti-patterns) + chordal distance to ideal pole on Fibonacci lattice",
  "parameters": {
    "basis": "internal-big-picture",
    "d": 9,
    "seed": 20260812
  },
  "delta": {
    "k": 1,
    "missing_primitives": [
      "examples",
      "guidelines",
      "constraints",
      "verification",
      "composition",
      "changelog",
      "references",
      "anti-patterns"
    ],
    "chordal_resid": 0.0
  },
  "verdict": "NO",
  "score": 6,
  "caveat": "binarization is heuristic; a stricter regex pass might surface sub-primitives"
}
```
