> **NOTE -- lens-external correction (added 2026-08-12)**:
> Lens metadata is stored EXTERNALLY in `lenses.json` ONLY (with `in_file_stamps: false`).
> Individual files (including `SKILL.md`) NEVER inline lens metadata.
> Patches generated by this cycle live in `.github/patches/<path>.patch` and ARE the
> deliverable -- source files are NOT also edited. This is the `patch-IS-the-change`
> discipline. Existing patch files in the tree are write-only and are NEVER edited.

# New ideas -- cycle-7 v2 (RSI NSS gap-informed atomic per-file, external-only)

**Skill:** curve-compass-skill v1.1.0 + curved-corpus-create v1.1.0 + negative-skill-space v1.0.0
**Cycle:** 7 v2 (RSI corpus audit, NSS 12-axis gap-finding BEFORE the fit, with lens-external / .patch-never-edit / patch-IS-the-change corrections)
**Date:** 2026-08-12
**Branch:** `feat/rsi-compass-cycle7-nss-research-2026-08-12-v2` (base: `c04900b85529`)

## Headline finding

Cycle 7 v2 fixes the three structural issues from PR #207 cleanup:

1. **lens-external**: lens metadata is stored externally in `lenses.json` only (with `in_file_stamps: false`); SKILL.md writers NEVER inline lens metadata.
2. **.patch-never-edit**: v2 never edits files inside `.github/patches/` (write-only mode); existing patch files in the tree are skipped.
3. **patch-IS-the-change**: when a skill generates a patch, the patch lives in `.github/patches/<path>.patch` and IS the deliverable; source files are NOT also edited.

Per Jenny's directive: **'deep research on a negative-skill-space before the fit'**. For each file, the 12-axis NSS sweep produces Extend gap candidates; the atom closes the highest-priority Extend gap with 1 atomic primitive flip per file (now as a patch file, not as inlined content).

**This PR adds 388 new ideas -- one per file -- each closing its highest-priority NSS Extend gap. The NSS gap-finding comes BEFORE the fit, so each atomic flip is informed by qualitative gap analysis, not just primitive-binarization. 388 patch files added under `.github/patches/`; source files unchanged.**

## Cycle progression (NSS-informed, with v2 corrections)

| Cycle | Patch type | Per-file changes |
|---|---|---|
| 0 (PR #195, prior session) | atomic per-file (single-action-curve-rsi) | 1 primitive flip per file |
| 1 (PR #202, declined) | templated sections | multiple generic sections per file |
| 2 (PR #203) | lens inlined per file | 30-line JSON code block per file |
| 3 (PR #204) | lens external (label only) | 5-line reference to `lenses.json` |
| 4 (PR #205) | new ideas = actual edits | multiple primitive flips per file |
| 5 (planned) | file-type-aware + richer infill | (deferred) |
| 6 (PR #206, MERGED) | atomic per-file (priority order) | 1 primitive flip per file |
| 7 (PR #207, CLEANED) | NSS gap-informed atomic per-file | 1 NSS-axis gap-flip each |
| **7 v2 (this PR)** | **NSS gap-informed atomic per-file + 3 corrections** | **388 files, 1 NSS-axis patch each (in `.github/patches/`)** |

## NSS gap-finding methodology (12-axis sweep)

| Axis | Heuristic patterns | Primitive mapped |
|---|---|---|
| audience | `\bfor users\b` ... | purpose (priority 7) |
| inputs | `\binput[s]?\b\s*:` ... | examples (priority 8) |
| outputs | `\boutput[s]?\b\s*:` ... | verification (priority 10) |
| mode | `\binteractive\b` ... | examples (priority 9) |
| assumption_set | `\brequires?\b` ... | constraints (priority 3) |
| adjacent_problems | `\brelated\b` ... | composition (priority 4) |
| failure_modes | `\bfails?\s+if\b` ... | anti-patterns (priority 2) |
| lifecycle | `\bchangelog\b` ... | changelog (priority 11) |
| composition | `\bimports?\b` ... | composition (priority 6) |
| knowledge_sources | `\breference[s]?\b` ... | references (priority 5) |
| calibration | `\bselftest\b` ... | verification (priority 1) |
| recursion | `\bself\b` ... | anti-patterns (priority 12) |

## NSS-axis gap distribution

| NSS axis | count | primitive flipped |
|---|---:|---|
| calibration | 101 | verification |
| failure_modes | 109 | anti-patterns |
| assumption_set | 63 | constraints |
| adjacent_problems | 57 | composition |
| knowledge_sources | 2 | references |
| audience | 47 | purpose |
| inputs | 4 | examples |
| mode | 4 | examples |
| recursion | 1 | anti-patterns |
| **total** | **388** | |

## Primitive distribution

| primitive | count |
|---|---:|
| purpose | 47 |
| examples | 8 |
| constraints | 63 |
| verification | 101 |
| composition | 57 |
| references | 2 |
| anti-patterns | 110 |
| **total** | **388** | |

## File-type distribution

| filetype | count |
|---|---:|
| conf | 28 |
| hcl | 1 |
| md | 280 |
| py | 20 |
| rego | 1 |
| sh | 55 |
| yaml | 3 |
| **total** | **388** |

## Per-category patch distribution

| category | files patched |
|---|---:|
| refs | 130 |
| skills | 104 |
| usr | 47 |
| tests | 28 |
| docs | 21 |
| scripts | 18 |
| (root) | 15 |
| .github | 8 |
| mkosi.conf.d | 8 |
| playbooks | 8 |
| mkosi.extra | 1 |
| **total** | **388** |

## Top 3 picks (highest score)

### L2001 -- `.dockerignore` (score 50/50, NSS axis `failure_modes`, primitive `anti-patterns`)
- **Hypothesis:** Closing the failure_modes NSS gap on .dockerignore (highest-priority among 11 gaps: ['audience', 'inputs', 'outputs', 'mode', 'assumption_set', 'adjacent_problems', 'failure_modes', 'lifecycle', 'composition', 'knowledge_sources', 'recursion']).
- **Method:** NSS 12-axis -> failure_modes (priority 2) -> atomic flip (anti-patterns); delivered as patch, source unchanged.
- **Delta:** NSS gaps 11 -> 10, axis closed: `failure_modes`, +8 lines (in `.github/patches/.dockerignore.patch`)
- **Verdict:** YES
- **Caveat:** NSS sweep is heuristic regex-based; LLM-as-judge would refine

### L2002 -- `.gitattributes` (score 50/50, NSS axis `calibration`, primitive `verification`)
- **Hypothesis:** Closing the calibration NSS gap on .gitattributes (highest-priority among 12 gaps: ['audience', 'inputs', 'outputs', 'mode', 'assumption_set', 'adjacent_problems', 'failure_modes', 'lifecycle', 'composition', 'knowledge_sources', 'calibration', 'recursion']).
- **Method:** NSS 12-axis -> calibration (priority 1) -> atomic flip (verification); delivered as patch, source unchanged.
- **Delta:** NSS gaps 12 -> 11, axis closed: `calibration`, +8 lines (in `.github/patches/.gitattributes.patch`)
- **Verdict:** YES
- **Caveat:** NSS sweep is heuristic regex-based; LLM-as-judge would refine

### L2003 -- `.github/CODE_OF_CONDUCT.md` (score 50/50, NSS axis `calibration`, primitive `verification`)
- **Hypothesis:** Closing the calibration NSS gap on .github/CODE_OF_CONDUCT.md (highest-priority among 9 gaps: ['audience', 'inputs', 'outputs', 'mode', 'assumption_set', 'adjacent_problems', 'knowledge_sources', 'calibration', 'recursion']).
- **Method:** NSS 12-axis -> calibration (priority 1) -> atomic flip (verification); delivered as patch, source unchanged.
- **Delta:** NSS gaps 9 -> 8, axis closed: `calibration`, +8 lines (in `.github/patches/.github/CODE_OF_CONDUCT.md.patch`)
- **Verdict:** YES
- **Caveat:** NSS sweep is heuristic regex-based; LLM-as-judge would refine

## Honest caveats

1. NSS sweep is heuristic regex-based -- LLM-as-judge would refine.
2. Priority order is opinionated -- calibration first (most actionable).
3. File-type detection is heuristic (cycle-5 fix preserved).
4. PR #206's per-file patches already addressed highest-priority primitive -- cycle 7 v2 may address a DIFFERENT gap.
5. Patch IS the deliverable -- source files are NOT edited; patches live in `.github/patches/<path>.patch` and must be applied separately (e.g. `git apply .github/patches/<path>.patch`).

## Jenny merges

Per PROJECT_RULES.md: this is a draft PR. Jenny reviews and merges; Sauna does not call `PUT /pulls/{n}/merge`.

## Audit trail

- Generated 2026-08-12 by RSI cycle-7 v2 (NSS gap-informed + 3 corrections).
- Base SHA: `c04900b85529017305b9fdc64bb5d9c817027445`.
- Lenses pool size: 388 (one per file with Extend gaps).
- Files patched: 388 (one NSS-axis atomic flip per file, delivered as `.github/patches/<path>.patch`).
- Total lines added (in patches): 2544.
- NSS sweep: 12 axes (Audience, Inputs, Outputs, Mode, Assumption set, Adjacent problems, Failure modes, Lifecycle, Composition, Knowledge sources, Calibration, Recursion).
- Seed: 20260812.
- v2 corrections applied: lens-external (lenses.json + new-ideas preamble), .patch-never-edit (write-only mode), patch-IS-the-change (patch in .github/patches/, source unchanged).
