diff --git a/a/crates/bcvk-qemu/src/credentials.rs b/b/crates/bcvk-qemu/src/credentials.rs
index 385f7bb..e5f84ee 100644
--- a/a/crates/bcvk-qemu/src/credentials.rs
+++ b/b/crates/bcvk-qemu/src/credentials.rs
@@ -241,6 +241,13 @@ mod tests {
         assert_eq!(smbios_cred_for_root_ssh(STUBKEY).unwrap(), expected);
     }
 
+    #[test]
+    fn test_karg_for_root_ssh() {
+        let b64_tmpfiles = BASE64.encode(key_to_root_tmpfiles_d(STUBKEY).as_bytes());
+        let expected = format!("systemd.set_credential_binary=tmpfiles.extra:{b64_tmpfiles}");
+        assert_eq!(karg_for_root_ssh(STUBKEY).unwrap(), expected);
+    }
+
     #[test]
     fn test_guest_path_to_unit_name() {
         assert_eq!(guest_path_to_unit_name("/mnt/data"), "mnt-data.mount");
diff --git a/a/crates/bcvk-qemu/src/lib.rs b/b/crates/bcvk-qemu/src/lib.rs
index 8f9583a..8832561 100644
--- a/a/crates/bcvk-qemu/src/lib.rs
+++ b/b/crates/bcvk-qemu/src/lib.rs
@@ -51,8 +51,8 @@ mod virtiofsd;
 
 pub use credentials::{
     generate_virtiofs_mount_unit, guest_path_to_unit_name, key_to_root_tmpfiles_d,
-    smbios_cred_for_root_ssh, smbios_cred_for_vsock_notify, smbios_creds_for_mount_unit,
-    smbios_creds_for_storage_opts, storage_opts_tmpfiles_d_lines,
+    karg_for_root_ssh, smbios_cred_for_root_ssh, smbios_cred_for_vsock_notify,
+    smbios_creds_for_mount_unit, smbios_creds_for_storage_opts, storage_opts_tmpfiles_d_lines,
 };
 
 pub use qemu::{
diff --git a/a/crates/kit/src/credentials.rs b/b/crates/kit/src/credentials.rs
index 41328ad..26d4de0 100644
--- a/a/crates/kit/src/credentials.rs
+++ b/b/crates/kit/src/credentials.rs
@@ -8,5 +8,6 @@
 // Re-export credential functions from bcvk-qemu that are used within kit
 pub use bcvk_qemu::{
     generate_virtiofs_mount_unit, guest_path_to_unit_name, key_to_root_tmpfiles_d,
-    smbios_cred_for_root_ssh, smbios_creds_for_storage_opts, storage_opts_tmpfiles_d_lines,
+    karg_for_root_ssh, smbios_cred_for_root_ssh, smbios_creds_for_storage_opts,
+    storage_opts_tmpfiles_d_lines,
 };
diff --git a/a/crates/kit/src/run_ephemeral.rs b/b/crates/kit/src/run_ephemeral.rs
index 2e41ff5..3f200c7 100644
--- a/a/crates/kit/src/run_ephemeral.rs
+++ b/b/crates/kit/src/run_ephemeral.rs
@@ -1699,13 +1699,20 @@ StandardOutput=file:/dev/virtio-ports/executestatus
     let vsock_force_disabled = std::env::var("BCVK_DEBUG").as_deref() == Ok("disable-vsock");
     let vsock_enabled = !vsock_force_disabled && qemu_config.enable_vsock().is_ok();
 
-    // Handle SSH key generation and credential injection
+    // Handle SSH key generation and credential injection. QEMU's ARM virt
+    // machine exposes SMBIOS tables through fw_cfg for firmware to publish.
+    // DirectBoot has no firmware, so pass this public key through systemd's
+    // documented kernel-command-line credential fallback on aarch64.
+    let mut ssh_credential_karg = None;
     if opts.common.ssh_keygen {
         let key_pair = crate::ssh::generate_default_keypair()?;
-        // Create credential and add to kernel args
         let pubkey = std::fs::read_to_string(key_pair.public_key_path.as_path())?;
-        let credential = crate::credentials::smbios_cred_for_root_ssh(&pubkey)?;
-        qemu_config.add_smbios_credential(credential);
+        if cfg!(target_arch = "aarch64") {
+            ssh_credential_karg = Some(crate::credentials::karg_for_root_ssh(&pubkey)?);
+        } else {
+            let credential = crate::credentials::smbios_cred_for_root_ssh(&pubkey)?;
+            qemu_config.add_smbios_credential(credential);
+        }
     }
 
     // YubiKey USB passthrough
@@ -1753,6 +1760,10 @@ StandardOutput=file:/dev/virtio-ports/executestatus
         kernel_cmdline.push("ignition.platform.id=qemu".to_string());
     }
 
+    if let Some(credential) = ssh_credential_karg {
+        kernel_cmdline.push(credential);
+    }
+
     kernel_cmdline.extend(opts.kernel_args.clone());
 
     // Software FIDO2/U2F (swu2f): QEMU cannot emulate a CTAP2 authenticator with the
