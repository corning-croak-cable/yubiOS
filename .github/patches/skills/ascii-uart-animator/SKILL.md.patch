# RSI cycle-7 v2 patch for skills/ascii-uart-animator/SKILL.md
# NSS axis: calibration (priority 1)
# Primitive: verification
# Lens metadata: external only (lenses.json) -- NOT inlined here.
# This patch IS the deliverable; source file unchanged.
# _RSI cycle-7 v2 atomic flip (gap-informed, NSS-axis(calibration))._
