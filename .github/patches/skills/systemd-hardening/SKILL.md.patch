# RSI cycle-7 v2 patch for skills/systemd-hardening/SKILL.md
# NSS axis: adjacent_problems (priority 4)
# Primitive: composition
# Lens metadata: external only (lenses.json) -- NOT inlined here.
# This patch IS the deliverable; source file unchanged.
# _RSI cycle-7 v2 atomic flip (gap-informed, NSS-axis(adjacent_problems))._
