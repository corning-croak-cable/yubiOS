# RSI cycle-7 v2 patch for skills/rsi-phi-skill/SKILL.md
# NSS axis: assumption_set (priority 3)
# Primitive: constraints
# Lens metadata: external only (lenses.json) -- NOT inlined here.
# This patch IS the deliverable; source file unchanged.
# _RSI cycle-7 v2 atomic flip (gap-informed, NSS-axis(assumption_set))._
