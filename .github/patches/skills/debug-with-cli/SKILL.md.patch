# RSI cycle-7 v2 patch for skills/debug-with-cli/SKILL.md
# NSS axis: knowledge_sources (priority 5)
# Primitive: references
# Lens metadata: external only (lenses.json) -- NOT inlined here.
# This patch IS the deliverable; source file unchanged.
# _RSI cycle-7 v2 atomic flip (gap-informed, NSS-axis(knowledge_sources))._
