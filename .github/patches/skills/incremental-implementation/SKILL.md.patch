# RSI cycle-7 v2 patch for skills/incremental-implementation/SKILL.md
# NSS axis: inputs (priority 8)
# Primitive: examples
# Lens metadata: external only (lenses.json) -- NOT inlined here.
# This patch IS the deliverable; source file unchanged.
# _RSI cycle-7 v2 atomic flip (gap-informed, NSS-axis(inputs))._
