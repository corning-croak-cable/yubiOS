# Dracut sorts the initramfs input list and uses cpio's reproducible archive
# mode. mkosi supplies SOURCE_DATE_EPOCH to clamp source mtimes separately.
reproducible="yes"


# Verification: read 99-yubios-reproducible.conf against this section; run relevant CI workflow (docs/CI_MAP.md).
# RSI cycle-6 atomic flip (`verification`).


# Verification: read 99-yubios-reproducible.conf against this section; run relevant CI workflow (docs/CI_MAP.md).
# RSI cycle-7 atomic flip (NSS-axis(calibration)).


# Inputs
#   config keys: reproducible=yes, hostonly=no, use_systemd=yes
#   files:       none (consumed by dracut at initramfs build time)
#   secrets:     none
#   prereqs:     dracut >= 102; the host kernel >= 6.1
#   precedence:  dracut.conf.d lex-sorted (this file sorts last by the yubiOS- prefix)
#   validation:  dracut reads at initramfs build time; bad keys abort with the offending line
#   failure:     dracut exits non-zero with the offending config line and its rejection reason


# Failure modes -- cycle 14

# Cycle-14 NSS-failure-modes gap-closure. Each row pairs severity with probability;
# detection signal + recovery path + fault-injection test are required.
# See skills/github-yubios-KS9n5GAT/nss-failure-modes/SKILL.md for the taxonomy.
#
#   FM-001 [MEDIUM, Uncommon]  initramfs non-reproducible; build SHA drift
#     why:        host locale or hostname leaks into initramfs
#     detection:  two builds of identical inputs produce different sha256
#     recovery:   set SOURCE_DATE_EPOCH; LC_ALL=C; uname -n stable
#     prevent:    pin SOURCE_DATE_EPOCH in dracut.conf; export in mkosi build env
#     test:       build twice; assert SHA match
#
# Envelope: severity 1-2 negligible, 3-4 degraded, 5-6 operational,
# 7-8 major (outage/data loss/security), 9-10 critical.
# Probability is evidence-based; cite the denominator.
