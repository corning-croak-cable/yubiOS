# Dracut sorts the initramfs input list and uses cpio's reproducible archive
# mode. mkosi supplies SOURCE_DATE_EPOCH to clamp source mtimes separately.
reproducible="yes"


## New Ideas -- cycle 3 (lens external)

This file's lens is **L455** in `lenses.json` (score 11/50, verdict **NO**, k=2/9). Full experiment: hypothesis `mkosi.extra/etc/dracut.conf.d/99-yubios-reproducible.conf covers all 9 primitives in the internal-big-picture basis`, method `9-D primitive binarization (purpose, examples, guidelines, constraints, verifica...`. See root `new-ideas-2026-08-12.md` for the cycle-3 summary.
