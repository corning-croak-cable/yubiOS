# Local two-build evidence is generated from committed inputs and retained by CI
# as a workflow artifact; it is not itself a source input.
repro-evidence/
reproducibility.json


## New Ideas -- cycle 3 (lens external)

This file's lens is **L489** in `lenses.json` (score 6/50, verdict **NO**, k=1/9). Full experiment: hypothesis `.gitignore covers all 9 primitives in the internal-big-picture basis`, method `9-D primitive binarization (purpose, examples, guidelines, constraints, verifica...`. See root `new-ideas-2026-08-12.md` for the cycle-3 summary.
